name: Publish package to npm

on:
  release:
    types:
      - published

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    name: Build and publish package
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup mise and install toolchain
        uses: jdx/mise-action@v2
        with:
          install: true
          cache: true

      - name: Show tool versions
        run: |
          node --version
          npm --version
          bun --version

      - name: Validate release tag against package version
        env:
          TAG_NAME: ${{ github.event.release.tag_name }}
        run: |
          TAG_VERSION="${TAG_NAME#v}"
          PACKAGE_VERSION="$(node -p "require('./package.json').version")"

          echo "Release tag    : ${TAG_NAME}"
          echo "Package version: ${PACKAGE_VERSION}"

          if [ "${TAG_VERSION}" != "${PACKAGE_VERSION}" ]; then
            echo "::error::Release tag (${TAG_NAME}) must match package.json version (${PACKAGE_VERSION})"
            exit 1
          fi

      - name: Install dependencies with Bun lockfile
        run: bun install --frozen-lockfile

      - name: Build package
        run: bun run build

      - name: Publish to npm
        run: npm publish
